import 'package:flutter/material.dart';

class TelegramAppPage extends StatelessWidget {
  const TelegramAppPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
            width: 150, height: 150, color: Color.fromARGB(185, 46, 231, 101)),
      ),
    );
  }
}
